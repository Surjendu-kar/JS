
const fruits = ["apple","banana","mango"];

for(let index in fruits){ // it is used to print the index 
    console.log(index);
}

// if you want to print the element using for in loop then ->
for(let i in fruits){  
    console.log(fruits[i]);
}
